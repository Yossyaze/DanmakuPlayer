---
description: Translate artifacts (task, walkthrough, implementation plan) to Japanese
---

1. Read the contents of `task.md`, `implementation_plan.md`, and `walkthrough.md` from the artifact directory (if they exist).
2. Translate the content of each file into natural Japanese suitable for software documentation.
   - Maintain all Markdown formatting (headers, lists, code blocks).
   - Preserve special states like `[ ]`, `[/]`, `[x]`, and alerts (e.g., `> [!NOTE]`).
3. Overwrite each file with its translated version.
4. Confirm to the user that the artifacts have been translated.
